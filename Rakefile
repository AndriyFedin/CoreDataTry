require 'rubygems'
require 'zake'

BetaBuilder::Tasks.new do |config|
  config.configuration = "Release"  
  config.target = "CoreDataTry"  
  config.build_dir = "Build"
  config.workspace_path = "CoreDataTry.xcworkspace"
  config.scheme         = "CoreDataTry"
  config.app_name       = "CoreDataTry" 
end
